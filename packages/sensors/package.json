{
  "name": "@maestro/sensors",
  "version": "0.0.1",
  "description": "Sensor registry, shell runner, and dispatcher",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "build:watch": "tsup --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "peerDependencies": {
    "@maestro/agents": "workspace:^"
  },
  "devDependencies": {
    "@maestro/agents": "workspace:^"
  },
  "dependencies": {
    "@maestro/config": "workspace:^",
    "@maestro/core": "workspace:^",
    "@maestro/sandbox": "workspace:^",
    "zod": "^4.3.6"
  }
}
