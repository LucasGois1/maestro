{
  "name": "@maestro/provider",
  "version": "0.0.1",
  "description": "AI SDK v6 wrapper for model-agnostic LLM access (Claude, OpenAI, Gemini, Ollama)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "build:watch": "tsup --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.71",
    "@ai-sdk/google": "^3.0.64",
    "@ai-sdk/openai": "^3.0.53",
    "@ai-sdk/provider": "^3.0.8",
    "@maestro/config": "workspace:*",
    "ai": "^6.0.168",
    "ai-sdk-ollama": "^3.8.3"
  }
}
