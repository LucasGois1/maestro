{
  "name": "@maestro/contract",
  "version": "0.0.1",
  "description": "Sprint contract schema, parser, and negotiation protocol",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "build:watch": "tsup --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@maestro/agents": "workspace:*",
    "@maestro/core": "workspace:*",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  }
}
