---
'@maestro/config': minor
'@maestro/cli': minor
---

Add configuration system: Zod schema, 3-level loader (global → project → run) with env-var overrides for secrets, secret masking, and `maestro config get/set/list/path/validate` commands.
